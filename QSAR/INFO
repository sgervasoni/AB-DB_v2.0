# The file included in the QSAR folder is needed to compute the descriptors of the Entry Rules:
# GOLB, PBF, RB, Primary amine

# The descriptors are computed with the python script calc_props.py, that needs a SMILES file as input
# To launch the script:
python calc_props.py -b opt.smi

# The opt.smi file is the SMILES file of the compound
